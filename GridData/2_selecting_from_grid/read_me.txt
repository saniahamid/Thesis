data1 have 5,00,000 points the program process_data.py takes thos points. divides the whole region into a grid of 200X200 unit of utm coordinates.
From each grid the pragram select multiples of 30 number of rides that star from that grid. The destination of these rides maybe on some other grid
but the program dosent take that into consideration.